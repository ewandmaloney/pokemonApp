import { HoverPokemonDirective } from './hover-pokemon.directive';

describe('HoverPokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPokemonDirective();
    expect(directive).toBeTruthy();
  });
});
